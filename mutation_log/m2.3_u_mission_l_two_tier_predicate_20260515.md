# mutation_log/m2.3_u_mission_l_two_tier_predicate_20260515.md

**Type:** field-map update / verification-structure extension (NOT hypothesis mutation)
**Authority:** Operator U-MISSION-L (2026-05-15 ~21:36 JST)
**Trigger:** Operator response to M2.3 Catch #2 halt-class finding surfaced at prior commit `7a28604`
**Mutation budget at M2 milestone-block:** 0/1 still consumed (this entry preserves the ratification precedent)
**Sequence position:** Resolves M2.3 Catch #2; advances U-MISSION-K from Step 2 (halted) through Steps 3 and 4 (predicate draft completed); HALT for operator ratification before M3.1 implementation per U-MISSION-L Step 4

---

## §1 Operator U-MISSION-L directive (verbatim)

```
Catch #2 resolution: ACCEPT Option A, with strengthening to two-tier predicate
(empirical + rigorous).

The folklore H ≈ 10^{P/n} relation is field standard practice in experimental
mathematics, not theorem-grade — operator-side framing in the previous reply
implicitly treated it as theorem-grade and that framing was wrong. H8 caught
not just a literature claim but a meta-claim about how to verify literature
claims. CLI correctly halted rather than retrofitting.

Two-tier predicate requirement (replaces single H_target in M2.3 predicate):

  Empirical scope claim (publishable, field-standard): "Null PSLQ at (P, n,
    maxcoeff) with cascade stability across 2P and 4P implies no integer
    relation with height ≤ H_empirical, where H_empirical follows BBC-
    calibrated empirical scaling H ≈ c · 10^{P/n} with c ≈ 2.06."
    Verification_class: field_standard_practice.

  Rigorous scope claim (theorem-grade fallback): "Per FBA 1999 Theorem 1 and
    Corollary 2, observed termination behavior of mpmath.pslq (iteration count
    k, final maxcoeff saturation, basis dimension n) implies rigorous null
    at H ≤ H_rigorous, where H_rigorous is derived from the available mpmath
    outputs without requiring H-matrix internals." Verification_class:
    proven_corollary.

Required CLI work to support the two-tier predicate:
  (a) Investigate what rigorous bound FBA Theorem 1 + Corollary 2 yield given
      only (k, final maxcoeff, n) — the outputs mpmath.pslq actually exposes.
      Produce harness/rigorous_bound.py with the derivation. ...
  (b) Budget: 1-2 days of CLI work on rigorous_bound.py. If the investigation
      surfaces that mpmath's available outputs genuinely cannot support any
      rigorous bound, that itself is a publishable observation about the state
      of PSLQ implementations in open-source libraries — note it for M6
      §Discussion.

H9: APPROVE with strengthenings:
  - Four verification classes, not three: rigorous_theorem, proven_corollary,
    field_standard_practice, empirical_heuristic. field_standard_practice
    covers community-validated empirical methods (BBC/BBP/Bailey-class)
    distinct from agent-derived empirical heuristics.
  - Any claim cited in M2.3 predicate, M3.1 harness, or M6 manuscript must
    carry a verification_class in its JSONL entry. Load-bearing classification.

(P, H_target) selection: ADOPT Option α (P=2160, H=10^70 BBC parity) for the
empirical scope claim. BBC's exact precision-per-dimension makes M6 directly
comparable. Cost differential vs Option β is negligible at 96h budget;
legitimacy gain is non-negligible. Option γ rejected. Option β rejected.

Mutation log: this resolution adds rigorous_bound.py investigation to the M3
scope. Classify as field-map update, not hypothesis mutation: the hypothesis
(test for integer relations in B_D(C)) is unchanged; the confidence reporting
structure is being extended. Mutation budget at M2: 0/1 still applies.

Sequence (non-collapsible):
  1. Install H9 with four classes per above. Update methodology/heuristics.md.
  2. Investigate FBA Theorem 1 + Corollary 2 yield from mpmath available outputs.
     Produce harness/rigorous_bound.py.
  3. Update harness/precision_budget.md §7 with the two-tier predicate text and
     the chosen (P=2160, H_empirical=10^70) pair, plus whatever H_rigorous
     emerges from step 2.
  4. Draft M2.3 final predicate text in literature/_m2.3_calibration_anchor.md
     §7 with three Excluded Families AND two-tier scope claim. Halt for
     operator ratification before M3.1 implementation.

Halt-and-flag pattern preserved. If step 2 surfaces a finding ... halt as
before. This is the third consecutive turn where the most important work has
been catching the previous turn's implicit assumptions; the pattern is mature
now and should be expected to continue rather than treated as exceptional.
```

---

## §2 Step-by-step execution record

### Step 1 — H9 installed with four classes

- File: `methodology/heuristics.md` H9 entry (full ~120-line section under "## H9 — Theorem-vs-heuristic classification on cited bounds").
- Four classes verbatim from operator: `rigorous_theorem`, `proven_corollary`, `field_standard_practice`, `empirical_heuristic`.
- Storage convention: `verification_class` as sub-key inside `independent_verifier_result` (preserves 7-field schema lock; validator unaffected).
- Retroactive-binding scope at install: lit-001, lit-002, lit-003, lit-009, lit-018 (the 5 entries load-bearing on M2.3 predicate per `_m2.3_calibration_anchor.md` §5 + §7).
- Forward-binding: on M2.3, M3.x, M5, M6.
- Sibling to H7 (capability functional verification) and H8 (literature paper-read verification); none generalizes any other.

### Step 2 — `harness/rigorous_bound.py` investigation

- File: `harness/rigorous_bound.py` (~16 KB module).
- Investigation outcome: **the rigorous bound IS meaningfully available from mpmath outputs** — via verbose=True stdout capture (Theorem 1 via mpmath's internal `Norm` printing) + Corollary 2 via iteration count.
- Four divergences from FBA 1999 documented in module docstring:
  - D1: mpmath uses `max_{i,j}|H[i,j]|`; FBA T1 uses `max_j|h_{j,j}|`. mpmath's bound is rigorous-but-strictly-weaker.
  - D2: mpmath uses γ = √(4/3) exactly; FBA Def 5 specifies γ > √(4/3) strict. Boundary case covered by Bailey 1998 (mpmath's stated source); Theorem 3 (overshoot) requires Bailey 1998 H8 paper-read if cited in M6.
  - D3: mpmath cites Bailey 1998, not FBA 1999, in docstring. Forward-flagged: Bailey 1998 H8 paper-read recommended before M3.1 if rigorous tier is load-bearing.
  - D4: mpmath applies `norm //= 100` safety factor. Conservative (rigor preserved); H_rigorous = 100 × reported_norm.
- **NO new halt was triggered.** None of D1-D4 invalidates Corollary 2 applicability per the operator's halt-criterion at Step 2 ("If step 2 surfaces a finding (e.g., mpmath.pslq's published behavior diverges from FBA 1999 in a way that affects Corollary 2 applicability), halt as before"). D2/D3 are forward-flagged for H8 strengthening at operator discretion; not halt-class.
- Demo evidence: `harness/_rigorous_bound_demo_output.txt` (n=15 mission basis at dps=100/500, maxsteps=2000/5000): K=5000 → H_rigorous = 1.43 × 10¹². Extrapolation: K ≈ 50-100k iterations reaches H_rigorous = 10⁷⁰ in ~150-200 s per run; well within 96h budget for thousands of runs.
- The originally-feared Option B Capability Gap CG-2 (rigid mpmath gap) is dissolved.

### Step 3 — `harness/precision_budget.md` updated for two-tier predicate

- File: `harness/precision_budget.md` (heavily rewritten).
- Status line: `STATUS: U-MISSION-L TWO-TIER PREDICATE INSTALLED` (was `HALT-CLASS FINDING SURFACED`).
- §5 revised: contradicts pre-U-MISSION-L "mpmath does NOT expose the rigorous certificate" reading; documents Theorem-1-style bound via verbose capture; 4 divergences in §5.1; achievable rigorous-bound regime in §5.2.
- §6.2 commits Option α (P=2160 dps, H_empirical=10⁷⁰).
- §6.3 records Options β and γ as rejected (with operator verbatim reasoning preserved).
- §7.3 records U-MISSION-L resolution (Option A with two-tier strengthening; CG-2 not declared; mutation budget 0/1 still applies).
- §8 references the predicate draft in `_m2.3_calibration_anchor.md` §7.
- §9 marks H9 INSTALLED with four classes.

### Step 4 — `literature/_m2.3_calibration_anchor.md` §7 — M2.3 final predicate text drafted

- File: `literature/_m2.3_calibration_anchor.md`.
- Previous §7 ("AEAL discipline note") renamed to §8.
- New §7 contents: §7.1 bounded sub-question restatement; §7.2 C-set fixed (verbatim U-MISSION-K); §7.3 three Excluded Families (EF1 BBC-grandfathered pure-power, EF2 signature-paper S_n, EF3 GKW-method-ceded); §7.4 positive predicate (success criterion: relation discovery in complement); §7.5 negative predicate (two-tier scope: H_empirical = 10⁷⁰ field_standard_practice + H_rigorous = max(100·final_norm, exp((K-2n³)/(2n²))) proven_corollary); §7.6 predicate failure modes; §7.7 outcome summary table; §7.8 operator ratification gate.
- Status: DRAFTED. AWAITING OPERATOR RATIFICATION before M3.1 implementation.

---

## §3 H9 retroactive-binding scope applied at this commit

`claims.jsonl` entries with `verification_class` sub-key added (inside `independent_verifier_result`):

| id | verification_class | rationale |
|---|---|---|
| lit-001-papanokechi-signature-pslq-2026 | `field_standard_practice` | EF2 anchor — operator's own prior published work |
| lit-002-bailey-borwein-crandall-1997 | `field_standard_practice` | EF1 anchor — BBC empirical calibration (c≈2.06) |
| lit-003-oeis-a002210 | `field_standard_practice` | OEIS canonical numerical record (K_0 value used in B_D(C)) |
| lit-009-ferguson-bailey-arno-1999 | `proven_corollary` | Rigorous tier — Theorem 1 + Corollary 2 via `harness/rigorous_bound.py` |
| lit-018-fidelity-no-prior-k0-pslq-refuted | `field_standard_practice` | M6 scope-positioning anchor (two-anchored legitimacy story) |

Other 15 entries (lit-004 to lit-008, lit-010 to lit-017, lit-019, lit-020) NOT in M2.3 dependency chain at this commit; per H8/H9 retroactive-binding clause, they remain without classification until they enter the chain.

Validator status: **PASS 20 entries, 0 errors** (validator unchanged; the 7-field schema is preserved; `verification_class` lives inside the free-form `independent_verifier_result` sub-dict).

---

## §4 What's NOT in this entry

- **No `selected.md` changes.** Frozen at gold/M1.
- **No `targets/` changes.** Frozen at gold/M1.
- **No new Capability Gap declared.** CG-2 (originally surfaced as Option B's escalation) is dissolved by the rigorous-bound investigation; mpmath's verbose mode suffices.
- **No M3.1 implementation begun.** Operator ratification of `_m2.3_calibration_anchor.md` §7 predicate text is the gate; CLI HALTS at Step 4 per U-MISSION-L non-collapsible sequence.
- **No new mutation_log entries except this one.** Field-map updates remain accounting-level, NOT hypothesis-level events.

---

## §5 AEAL discipline notes

1. **Mutation budget at M2 milestone-block: 0/1 still consumed.** Per operator U-MISSION-L verbatim: "Classify as field-map update, not hypothesis mutation: the hypothesis (test for integer relations in B_D(C)) is unchanged; the confidence reporting structure is being extended." The two-tier predicate is a confidence-reporting extension, not a hypothesis change. H9 install is a governance addition (sibling to H7/H8), not a mutation. The `verification_class` additions are field-map updates inside the free-form `independent_verifier_result` sub-dict, not schema changes.

2. **The Catch #2 → U-MISSION-L cycle counts as ONE catch in the AEAL maturation curve.** Per operator U-MISSION-L verbatim: "This is the third consecutive turn where the most important work has been catching the previous turn's implicit assumptions; the pattern is mature now and should be expected to continue rather than treated as exceptional." M2.2 had 0 findings; M2.3 surfaced exactly 1 finding (Catch #2); the resolution surfaced no new halt at Step 2 (rigorous_bound.py investigation succeeded). The mission's halt-and-flag pattern remains the invariant; the absence of additional halts at U-MISSION-L resolution is consistent with the AEAL maturation curve framing.

3. **Forward-flagged for downstream review (not blocking):** Bailey 1998 expository paper enters H8 dependency chain IF M6 manuscript explicitly cites the rigorous tier of the predicate as proof-of-bound. Currently the rigorous tier is a CLI internal claim with verification_class `proven_corollary` derived from FBA 1999 (already H8-verified); Bailey 1998 H8 paper-read is recommended only if M6 reception requires the boundary γ = √(4/3) case to be rigorously sourced.

4. **The pre-U-MISSION-L Option B (Capability Gap CG-2 declaration) is preserved in `precision_budget.md` §7.3 as a "what would have happened" branch.** If operator later determines that the four divergences D1-D4 are unacceptable (e.g., wants a non-mpmath PSLQ implementation that exposes diagonals natively), Option B may be revisited as a future Capability Gap event — but not at this commit.

5. **Per-commit Conflict-log entry:** none. H7, H8, H9 are co-binding without conflict at this commit; all retroactive scopes are honored; no operator authority overrides the heuristics.

---

## §6 Cross-references

- `methodology/heuristics.md` H9 entry (this commit) — four-class taxonomy installed
- `harness/precision_budget.md` (this commit, heavily revised) — technical anchor + Option α commitment
- `harness/rigorous_bound.py` (this commit, NEW) — Tier-2 implementation
- `harness/_rigorous_bound_demo_output.txt` (this commit, NEW) — Tier-2 evidence
- `literature/_m2.3_calibration_anchor.md` §7 (this commit, NEW) — M2.3 final predicate text awaiting ratification
- `literature/_fidelity_findings.md` §6 (this commit, updated) — Catch #2 audit trail extended with U-MISSION-L resolution
- `literature/claims.jsonl` (this commit, updated) — 5 entries with `verification_class` sub-key
- `mutation_log/m2.2_to_m2.3_pslq_certificate_halt_20260515.md` (prior commit `7a28604`) — Catch #2 halt-event log (predecessor to this entry)
- Operator U-MISSION-K (2026-05-15 ~21:14 JST) — original non-collapsible 4-step sequence
- Operator U-MISSION-L (2026-05-15 ~21:36 JST) — resolution directive
