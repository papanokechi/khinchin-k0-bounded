# mutation_log/m3.2_phase_split_20260516.md

**Date:** 2026-05-16 ~08:22 JST
**Trigger:** Operator U-MISSION-M3.2 — "M3.2 GREENLIGHT — split into phases M3.2a and M3.2b."
**Author:** CLI (Claude Opus 4.7 extra-high, this session)
**Classification:** Field-map update / procedural extension (NOT a hypothesis mutation).
**Mutation budget at M2:** 0/1 still consumed (unchanged).

---

## 1. Summary

The M2.3 final predicate text (`literature/_m2.3_calibration_anchor.md` §7), frozen at `gold/M2` tag (commit `ca9c989`), is extended with a new §7.9 documenting the M3.2 execution phase split into M3.2a (single canonical primary cascade, gated) and M3.2b (64-sub-basis empirical sweep, gated on M3.2a clean outcome).

This entry exists because the gold/M2 freeze rule (per CLI's own task_complete summary after gold/M2 was tagged) states: *"Edits to §7 after gold/M2 require explicit mutation_log entry."* The §7.9 addition is operator-authorized post-freeze procedural refinement, not a hypothesis change.

## 2. What changed

### 2.1 `literature/_m2.3_calibration_anchor.md` (predicate document)

- §7.8 unchanged narrative; new closing block adds RATIFIED status timestamp + commit hash + tag location.
- **NEW §7.9 inserted** documenting:
  - M3.2a: single canonical primary cascade, rigorous tier, output `m32a_primary_cascade.jsonl`, gated by `--m32-primary-greenlighted`
  - Halt-and-flag review of M3.2a with four clean-null acceptance criteria
  - M3.2b: gated empirical sweep, conditional on M3.2a clean outcome
  - Second-leg precision correction: `gp_lindep` at `dps = primary_P` (not `primary_P // 2`)

### 2.2 `harness/verify.py`

- `gp_lindep_verify`: signature extended to accept `H_empirical` parameter for in-call interpretation; timeout default raised from 180s to 1800s for full-precision LLL on n=15 basis.
- `verify_candidate`: gp leg now invoked at `dps = primary_P` (not `primary_P // 2`); `H_empirical` computed before gp call and threaded through; `gp_result` post-augmented with `max_abs_coefficient`, `within_empirical_bound`, `gp_verdict` fields.
- New `_summarize_gp_verdict` helper: combines cascade verdict + gp leg result into a single second-leg verdict string for operator surfacing.
- `LegResult` dataclass: extended with `termination_reason`, `termination_line`, `last_norm_in_cancellation`, `verbose_lines` fields (mpmath verbose H-matrix-diagonals-proxy summary per operator M3.2a surfacing requirement).
- `cascade_pslq` per_precision dict: now includes the new LegResult fields for downstream M3.2a review.

### 2.3 `harness/sweep.py`

- `--primary-only` mode REPLACED by two distinct flags per operator directive:
  - `--m31-extended-dry-run`: primary cascade at reduced precision (P=540/1080/2160), ungated, JSONL marked `dry_run: true`
  - `--m32-primary-measurement`: canonical primary cascade at full precision (P=2160/4320/8640), gated by `--m32-primary-greenlighted`, JSONL marked `dry_run: false`, output to canonical filename `m32a_primary_cascade.jsonl`
- `--m32-greenlighted` flag REPLACED by two distinct flags:
  - `--m32-primary-greenlighted` (M3.2a gate)
  - `--m32-full-greenlighted` (M3.2b gate)
- `--full` mode: now requires BOTH `--m32-full-greenlighted` AND prior presence of `m32a_primary_cascade.jsonl` with non-meta records. Added `_verify_m32a_prereq()` check at argparse layer.
- Canonical output filenames installed (`m32a_primary_cascade.jsonl`, `m32b_empirical_sweep.jsonl`) with refuse-to-overwrite semantics (single canonical execution).
- JSONL header now includes `dry_run: true|false` and `canonical: true|false` for downstream filtering.

### 2.4 `harness/sweep_output/README.md`

- Mode table updated to reflect M3.2 phase split.
- Per-candidate schema documentation updated with new gp_lindep keys (`max_abs_coefficient`, `within_empirical_bound`, `gp_verdict`) and new cascade.per_precision keys (`termination_reason`, etc.).
- gp_lindep interpretation guidance: "PARI/GP `lindep` performs LLL and always returns a 'best approximate' integer combination ... `within_empirical_bound = max|m_i| <= H_empirical`."

## 3. Why this is procedural extension, not hypothesis mutation

Per operator U-MISSION-M3.2 verbatim:
> "Not a mutation (no hypothesis change); a procedural extension of the predicate text. Mutation budget unchanged."

The hypothesis under test — "test for integer relations in B_D(C) with three Excluded Families" — is unchanged. What changed:

1. **How** the predicate is observed (one canonical run → two phased canonical runs with a review gate between).
2. **What** the second-leg precision is (primary_P // 2 → primary_P; corrects an asymmetry that operator caught at greenlight time).
3. **What** is surfaced from each candidate (mpmath verbose summary fields now structured rather than free-form).

These are observation-protocol refinements. The predicate text in §7.1-§7.7 (Excluded Families, two-tier scope claim, H_empirical=10⁷⁰, H_rigorous formula, JSONL schema) is BYTE-UNCHANGED.

## 4. Authority chain

- `mutation_log/m1.2_to_m1.3_brief_fidelity_correction.md` (operator-side error in brief, frame correction)
- `mutation_log/m1.3_pari_gp_install_20260515.md` (H1 install, H7 install)
- `mutation_log/m1.3_pari_gp_install_h7_retroactive_20260515.md` (H7 retroactive verification)
- `mutation_log/m2.1_lit_018_signature_paper_field_map_update.md` (lit-018 absorption)
- `mutation_log/m2.1_lit_009_h8_field_map_update_20260515.md` (BBC-1997 paper-read upgrade)
- `mutation_log/m2.2_capability_audit_close_20260515.md` (capability audit clean close, no fidelity catch)
- `mutation_log/m2.3_u_mission_l_two_tier_predicate_20260515.md` (U-MISSION-L two-tier predicate install, H9 install)
- **THIS ENTRY** (`mutation_log/m3.2_phase_split_20260516.md`) — 8th mutation_log entry; M3.2 phase split + second-leg precision correction.

## 5. Rule 6 audit

- Zero portal interactions.
- Single planned `git push origin main` will execute at this commit.
- `selected.md`: not touched (frozen at gold/M1).
- `_m2.3_calibration_anchor.md` §7.1-§7.7: BYTE-UNCHANGED (frozen at gold/M2).
- §7.8: closing-block addition (RATIFIED status + commit hash + tag location); does not modify the existing ratification-gate text.
- §7.9: NEW subsection added per operator authorization.
- `claims.jsonl`: not touched.
- `methodology/heuristics.md`: not touched (H9 stays as installed at U-MISSION-L).
- Mutation budget: 0/1 remains consumed at M2 (no hypothesis change).
- New tag (e.g., `gold/M3a` or similar): NOT applied at this commit; operator may tag at M3.2a clean review if desired.

## 6. Halt-and-flag pattern preservation

Operator verbatim from U-MISSION-M3.2: *"The M3.2a measurement is the most consequential 90 minutes of the mission to date. Halt if anything surprises."*

The argparse gate `--m32-full-greenlighted` requiring prior `m32a_primary_cascade.jsonl` codifies the halt-and-flag pattern at the file system level: even with explicit operator greenlight for `--full`, the harness will refuse to execute M3.2b without M3.2a evidence on disk. This makes "halt for M3.2a operator review" physically enforced, not just a CLI convention.

## 7. Files in this commit batch (pre-M3.2a)

| File | Action |
|---|---|
| `literature/_m2.3_calibration_anchor.md` | Edit (§7.8 status block + §7.9 new) |
| `harness/verify.py` | Edit (gp dps + H_empirical threading + gp_verdict + LegResult extension) |
| `harness/sweep.py` | Replace (full rewrite for new mode structure) |
| `harness/sweep_output/README.md` | Edit (mode table + schema docs) |
| `harness/_smoke_gp_full_precision.py` | Create (audit-trail: gp at P=2160 wall-clock verification) |
| `harness/_inspect_dryrun.py` | Edit (handles canonical files + new fields) |
| `mutation_log/m3.2_phase_split_20260516.md` | Create (this entry) |
| `plan.md` (session-local) | Out-of-tree addendum |

The canonical M3.2a output (`m32a_primary_cascade.jsonl`) will land in a SEPARATE commit immediately after `--m32-primary-measurement` execution completes, so that the pre-execution corrections commit and the canonical measurement artifact are bisectable as distinct events.
