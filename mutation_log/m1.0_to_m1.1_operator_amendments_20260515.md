# Mutation log — M1.0 → M1.1 transition with operator amendments

**Date:** 2026-05-15 ~17:38 JST
**Trigger:** Operator review of M1.0 §9 first-action deliverables (~17:38 JST chat message)
**Brief reference:** §7 *"One hypothesis mutation per milestone, logged in `mutation_log/`."*
**Mutation class:** SURVEY SET AMENDMENT + MACHINERY-BASE EMPIRICAL CONFIRMATION + LINKAGE RETRACTION

## Operator directives (verbatim from chat)

> U-MISSION-A: approve 27 of 30. Drop #10 (odd perfect). Hold #18 (Lévy's β) pending literature recheck — return result before M1.1 scoring. Hold #23, #26 pending one-paragraph `overlap_audit.md` each. Proceed to M1.1 row construction on the 27 clear entries.
>
> U-MISSION-B: deferred. Re-raise after M1.2 target selection, before M1.3 Gold Freeze G1. Continue file-system staging only.
>
> U-MISSION-C: run capability probe first. `python -c "import arb, flint, fpylll, cypari2"` plus `which gp` and `lean --version`. Return probe output; I'll confirm machinery base from actual results.
>
> Item 28 / external_validation_deficit linkage noted. Confirm or retract in your next reply based on whether the JNT comment text actually supports the connection.

## Mutations applied (CLI actions)

### A. Survey-set amendments (per U-MISSION-A)

**Dropped from M1.1 input:**
- #10 Odd perfect numbers — operator-instructed drop. CLI concurs: extending odd-perfect lower bounds requires bespoke factorization machinery (parity-decomposition + Euler's-form-constraint propagation + heavy ECM/NFS) that is outside the confirmed machinery base. Honest disposition.

**Held → cleared after recheck/audit:**
- #18 Lévy's β — literature recheck complete (`targets/literature_recheck_18_levy_constant.md`). Both irrationality and transcendence remain genuinely OPEN as of 2024-2025. Entry SURVIVES.
- #23 Stanley plane-partition constants — overlap audit complete (`targets/overlap_audit_23_stanley_plane_partition.md`). Machinery overlap with active SIARC tracks, but NO content overlap; attribution would be clean. Entry SURVIVES.

**Held → recommended DROP after audit:**
- #26 Gauss–Kuzmin effective error terms — overlap audit complete (`targets/overlap_audit_26_gauss_kuzmin.md`). Content-axis overlap with active Finite-Depth Transient Rigidity manuscript is SEVERE (same Gauss–Kuzmin–Wirsing operator, same CF-dynamics content domain, same literature base). A positive result would muddy attribution and amplify the JNT-referee `external_validation_deficit` signal. CLI RECOMMENDS DROP.

**Net survey-set disposition:**
- 26 entries advance unconditionally to M1.1: #1-9, #11-17, #19-22, #24-25, #27-30
- 1 entry advances after recheck (#18): cleared
- **27 advance to M1.1** in total (matches operator's "27 of 30" count)
- 2 dropped (#10 by operator, #26 by CLI recommendation pending operator confirmation)
- 1 was held by operator on machinery edge: not changed here, but flagged that #26 drop reduces from 27 to 26 IF operator accepts CLI recommendation

### B. Machinery base — empirically confirmed (per U-MISSION-C)

Capability probe executed 2026-05-15 ~17:38 JST. Full results: `capability/probe_results_20260515.md`. Canonical machinery list: `capability/machinery_base_confirmed.md`.

**Material refinement from M1.0 inference:**
- `python-flint` / arb ball arithmetic: NOT AVAILABLE (was "if exposed" in M1.0 inference, now confirmed absent)
- `fpylll`: NOT AVAILABLE (was unmentioned in M1.0)
- `cypari2`: NOT AVAILABLE (was unmentioned in M1.0)
- `sagemath`: NOT AVAILABLE in any flavor
- `scipy`: NOT AVAILABLE (was implicit-assumed available; corrected)
- PARI/GP via `gp` binary shell-out: AVAILABLE — this is the only PARI path; no Python bridge
- mpmath 1.3.0 PSLQ at 500 dps: CONFIRMED working

**Structural consequence:** the M3.1 verification harness "second-library leg" cannot be `mpmath + arb` (per Brief §M3.1 example phrasing) and cannot be `sympy + sage`. Substitute: `mpmath PSLQ` (primary) + `gp lindep via shell-out` (independent leg) + optional `sympy.nsimplify` (sanity tie-breaker). This is documented in `capability/machinery_base_confirmed.md`. It is a constraint on the harness design, NOT a capability gap on the mission as a whole.

**Operator review point:** if the operator wants the canonical `mpmath + arb` pair, they can `pip install python-flint`. CLI did NOT propose this install. The honest position is that the mission runs on the AS-IS machinery base.

### C. Item 28 / external_validation_deficit linkage — RETRACTED (strong form)

**Original M1.0 §9 claim (retracted):** *"design-coherence with today's Item 28 JNT `external_validation_deficit` finding — bounded AEAL discipline is well-suited to address the Searcher's Fatigue failure mode that may have contributed to the rejection pattern."*

**Retraction reasoning:**
- Re-read Item 28 verbatim referee text from `submission_log.txt` lines 271-312: *"Given the strength of other submissions and the fact that most of the recent references are to work of the author (companion paper, preprint), the paper is not a good fit for JNT and should be sent elsewhere."*
- The JNT referee is diagnosing **citation-graph self-loop dominance** — an EXTERNAL perception of research-program isolation.
- AEAL's "Searcher's Fatigue" (Brief §0.2) is **substitution of compute for reasoning** — an INTERNAL methodology failure mode.
- These are **distinct failure modes**. They can correlate (a Fatigue-driven program might also produce a self-citation graph by spawning companion preprints), but they are not the same; AEAL discipline does NOT directly address citation-graph topology.

**Replacement claim (refined, honest):**
> AEAL v1 discipline directly addresses Searcher's Fatigue by construction (per Brief §0.2). It does **NOT** directly address the JNT-referee-observed `external_validation_deficit` (citation-graph self-loop dominance). However, AEAL deliverables (reproducible scripts, machine-checkable success predicates, capability-gap documentation, Zenodo deposits with formal Lean checks) produce evidence of a different kind than community engagement — evidence of methodological rigor that may partially mitigate, but cannot replace, the citation-graph signal. The unsolved-relay mission's deliverable is a Zenodo deposit + manuscript whose credibility rests on AEAL provenance, not on a wider citation network. This is a distinct credibility axis, not a substitute for the missing one.

**Implication for mission framing:** unsolved-relay should NOT be marketed (internally or externally) as a solution to the rejection pattern. It is one limited credibility-building exercise on one axis (methodological rigor). The other axis (external citation network) requires separate strategy (e.g., the operator's coauthorship-probe paper14 strategy), which is unaffected by this mission.

### D. U-MISSION-B deferral (per operator)

Repo creation `gh repo create papanokechi/unsolved-relay` is DEFERRED until post-M1.2 target selection. Rationale (operator-supplied + CLI-concur): the chosen target may justify a more specific repo name; creating now commits to the generic name before the target is known. CLI continues file-system staging only at `siarc\unsolved-relay-staging\`.

## AEAL audit of this mutation

- **Claim-to-sweep ratio:** this mutation log introduces 0 claims and 0 sweeps. Pure governance documentation. Score: N/A.
- **Reproduce-command coverage:** the capability probe has an inline reproduce script. The literature recheck has a web-search reproduce note. The overlap audits cite the relevant SIARC manuscripts by name. Score: HIGH.
- **Capability-gap honesty:** this mutation EXPANDED the known capability-gap surface by confirming arb/fpylll/cypari2/sage as absent. Score: HIGH.
- **Mutation count:** ONE mutation event per milestone, logged. Compliant with Brief §7.

## Post-mutation state

| Survey entry status | Count |
|---|---|
| Advancing to M1.1 (unconditional) | 26 (#1-9, #11-17, #19-22, #24-25, #27-30) |
| Advancing to M1.1 (cleared via recheck #18) | 1 (#18) |
| Dropped by operator | 1 (#10) |
| Dropped by CLI recommendation, awaiting operator confirmation | 1 (#26) |
| **TOTAL ADVANCING IF OPERATOR ACCEPTS #26 DROP** | **27** |
| **TOTAL ADVANCING IF OPERATOR OVERRULES #26 DROP** | **28** (with #26 reworded per audit) |

CLI proceeds to construct `targets/triage.json` rows for the 27 unambiguously-clear entries (#1-9, #11-17, #18, #19-22, #24-25, #27-30) now. The #26 row is held pending operator confirmation of the audit recommendation.

## Cross-reference

- `capability/probe_results_20260515.md` — empirical probe output
- `capability/machinery_base_confirmed.md` — canonical machinery list
- `targets/literature_recheck_18_levy_constant.md` — #18 recheck artefact
- `targets/overlap_audit_23_stanley_plane_partition.md` — #23 audit (PASS)
- `targets/overlap_audit_26_gauss_kuzmin.md` — #26 audit (RECOMMEND DROP)
- `targets/survey_set.md` — to be updated with the amendments-applied footer
- `targets/triage.json` — NEXT to be built (27 rows)
- `_M1.0_first_action_log.md` — superseded; do not edit; preserved as the §9 snapshot
