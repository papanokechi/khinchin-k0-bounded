# U-MISSION-J: H8 install + M2.1→M2.2 decisions (2026-05-15 ~21:00 JST)

**Event class:** frame-correction + heuristic-install + operator-binding-requirement-add.
**Mutation budget consumption:** **0 of 1 at M2 milestone-block** (operator-ratified: lit-018 catch is a documentation event, not a hypothesis mutation; H8 install is a heuristic addition subordinate to AEAL Brief §M2.1, sibling-to-H7 install pattern; M2.3 binding requirement is a calibration constraint on the predicate text, not a basis change).
**Triggering event:** operator U-MISSION-J reply at 2026-05-15 ~21:00 JST in response to CLI's M2.1 closure (commit `c7dfce9` on `main` of `papanokechi/khinchin-k0-bounded`).

---

## Operator directive (verbatim, full text)

> U-MISSION-J: APPROVE H8 with extended scope. Proposed wording:
> **H8 — Paper-read verification on dependency literature claims.** Any literature claim that the M2.3 success predicate, the M3.1 harness design, or the M6 manuscript's scope-distinction story depends on must be verified by direct reading of the primary source (paper, preprint, supplementary material). Web aggregators, citation databases, and LLM-mediated summaries are not sufficient and can produce confident false positives in both directions (false existence and false non-existence, sometimes both for the same claim). For paywalled or non-digitized primary sources, mark `verified_independently: false` with the specific blocker and do not propagate the claim into the predicate. Subordinate to Brief §M2.1; sibling to H7. Retroactively binding only on entries currently relied on by M2.3 calibration; entries not yet relied on may remain in their current verification state until they enter the dependency chain.
>
> Halt-status: CONFIRMED no 5th-class halt. BBC-1997 finding confirms M1.2 sub-question scope rather than contradicting it. `selected.md` remains frozen.
>
> M2.3 binding requirement: the success predicate must explicitly exclude the pure-power family {1, K_0, K_0^2, ..., K_0^D} tested in BBC 1997 §4 Test 1 (dim 51, D≤50, H≤10^70, 7350 dps). The hybrid basis B_D(C) remains; the pure-power-only subset is named as grandfathered prior art and the predicate scope is the complement. M2.3 calibration anchor already implies this; M2.3 final predicate text must state it explicitly.
>
> Mutation budget ratification: lit-018 discovery is correctly classified as a documentation event, not a hypothesis mutation. §7 budget consumption is reserved for changes to the operator's hypothesis. Refinements to the prior-work field map under H6/H8 are field-map updates, not hypothesis mutations. Precedent: this distinction holds going forward unless a future discovery forces a hypothesis change (e.g., a paper that has tested the full B_D(C) hybrid basis would force a mutation).
>
> Scope-positioning note for M6: the manuscript now has a two-anchored legitimacy story. khinchin-signature-pslq (operator's own 2026-04-17 paper) handed off algebraic-combination relations; BBC 1997 §4 explicitly named log K_0, (log K_0)(log 2), and "many other forms involving K_0" as open. The mission's refined sub-question sits inside both handoffs. This is the strongest possible scope-positioning for a partial-result paper and should be cited explicitly in M6 §Introduction.
>
> M2.2 GREENLIGHT. Proceed to capability audit. Apply H7 functional verification to each technique cited in M2.1 entries that the harness will use. Apply H8 paper-read verification to any computational technique whose published claims the harness will rely on (e.g., if any M2.1 entry asserts a specific PSLQ precision-to-confidence relation that the M3.1 harness will calibrate against, paper-read verify before propagation).

---

## Resolution and execution

### Action 1 — Install H8 in `methodology/heuristics.md`

- **Done.** H8 added as sibling to H7 (NOT generalization). Operator wording preserved verbatim in the heuristic body. Header `≤ 7 entries` → `≤ 8 entries`. Health-score footer updated to attach H8 to a fourth axis: **literature-fidelity honesty** (sibling to H7's capability-gap-honesty axis).
- File: `methodology/heuristics.md`.
- Pre-install draft preserved in `literature/_fidelity_findings.md` §2 with explicit annotation that operator's installed version extends scope beyond the CLI draft (operator added M6 manuscript scope-distinction story to the dependency-triad; operator added "false positives in both directions" failure-mode framing).

### Action 2 — M2.3 binding requirement → `_m2.3_calibration_anchor.md` §6.5

- **Done.** Operator's verbatim binding text added at §6.5. CLI proposal text at §3 reaffirmed as consistent with the binding (i.e., the §3 proposal is no longer a CLI proposal subject to operator review at M2.3 — its **structural content** is operator-bound).
- The pure-power-only subset {1, K_0, K_0², …, K_0^D : D ≤ 6} is named **grandfathered prior art** of BBC 1997 §4 Test 1; the M2.3 success predicate's scope is the **complement** within the hybrid basis B_D(C).

### Action 3 — M6 scope-positioning note → `_m2.3_calibration_anchor.md` §6.6

- **Done.** Operator's verbatim "two-anchored legitimacy story" added as §6.6 with the two anchor quotes pulled from lit-001 (signature paper) and lit-002 (BBC 1997 §4 close), and the M6 §Introduction citation obligation specified.

### Action 4 — Halt-status decision → `lit-018-...md` §7

- **Done.** §7 updated from "operator decision pending" to "RESOLVED no 5th-class halt." `selected.md` remains frozen. The lit-018 entry's AEAL-schema `status` field remains `fidelity_caught_refuted` (the catch documentation stands).

### Action 5 — Mutation budget ratification

- **Recorded here.** This entry IS the precedent record. Going forward:
  - H6/H8 prior-work field-map updates: NOT mutations.
  - Documentation events (e.g., catch entries in `_fidelity_findings.md`): NOT mutations.
  - Refinements to the M2.3 calibration anchor that consume only operator-bound directives (like the §6.5 install): NOT mutations.
  - Future mutation triggers (operator-named example): "a paper that has tested the full B_D(C) hybrid basis would force a mutation." If such a discovery occurs, mutation budget IS consumed at that point.

### Action 6 — M2.2 GREENLIGHT execution

- **Done.** `capability/m2.2_capability_audit.md` written with H7 functional verifications (per operator: "Apply H7 functional verification to each technique cited in M2.1 entries that the harness will use") and H8 paper-read trace (per operator: "Apply H8 paper-read verification to any computational technique whose published claims the harness will rely on").
- Outcome: all M3.1-relied techniques PASS H7. The single technique with a potential H8 dep-chain entry (mpmath.pslq → algorithm bounds of Ferguson-Bailey-Arno 1999, lit-009) is documented as NOT YET in the dep chain — M3.1 harness uses `mpmath.pslq` semantics directly, does not cite specific Ferguson-Bailey-Arno coefficient bounds. If M2.3 predicate text or M6 manuscript ends up citing a specific PSLQ bound, lit-009 must be paper-read at that point.

---

## Brief-fidelity classification

This entry is a **frame correction + heuristic install + operator-binding requirement add**, NOT a hypothesis mutation. Per operator's mutation-budget ratification (Action 5 above), the mutation budget consumption for U-MISSION-J at the M2.1 → M2.2 boundary is **0 of 1**.

Cf. precedents:
- `m1.0_to_m1.1_operator_amendments_20260515.md` — 0-mutation operator amendments at boundary.
- `m1.1_to_m1.2_shortlist_construction_20260515.md` — 0-mutation in-milestone process.
- `m1.2_to_m1.3_brief_fidelity_correction.md` — frame correction (factual error in Brief), NOT mutation.
- `m1.3_pari_gp_install_20260515.md` — H7 install + capability retroactive verification, NOT mutation.
- **THIS ENTRY (m2.1_to_m2.2_u_mission_j_h8_install_20260515.md) — H8 install + M2.3 binding requirement add + halt-status confirmation + mutation-budget-ratification precedent install + M6 scope-positioning capture + M2.2 greenlight execution, NOT mutation.**

---

## State after this entry

- `methodology/heuristics.md` — H1–H8 installed; ≤ 8 cap; health-score footer covers four axes.
- `literature/_m2.3_calibration_anchor.md` — §3 proposal reaffirmed as operator-bound at §6.5; M6 scope-positioning captured at §6.6.
- `literature/_fidelity_findings.md` §2 — H8 marked INSTALLED (was: PROPOSED).
- `literature/lit-018-...md` §7 — RESOLVED (was: operator decision pending).
- `capability/m2.2_capability_audit.md` — NEW M2.2 deliverable.
- This `mutation_log/` entry — NEW.
- `selected.md` — UNCHANGED (frozen at gold/M1; the M2.3 calibration anchor §6.5 is a DOWNSTREAM consumer document, NOT a mutation of selected.md per operator ratification).

**`gold/M1` tag remains at commit `15b216f3ba7a1b75dd7665d31ad39ac66d8bf17a` (frozen).**
**`main` branch advances with this M2.2 commit.**

---

## Operator quote, retained for governance precedent

> "Refinements to the prior-work field map under H6/H8 are field-map updates, not hypothesis mutations. Precedent: this distinction holds going forward unless a future discovery forces a hypothesis change (e.g., a paper that has tested the full B_D(C) hybrid basis would force a mutation)."

This is the **operative test** for future budget-consumption decisions: does the discovery force a change to the operator's hypothesis (basis form, predicate shape, success criterion)? If yes → mutation. If no (only refines understanding of the prior-work field around an unchanged hypothesis) → field-map update, NOT mutation.
