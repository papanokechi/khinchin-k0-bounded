# M2.3 Calibration Anchor — BBC 1997 Grandfathering of the Pure-Power Sub-basis

**Created:** M2.1 literature ledger construction, 2026-05-15
**Status:** binding on M2.3 success-predicate definition; informative for M3.1 harness implementation
**Source:** lit-002 (BBC 1997 §4 paper-read-verified, see `lit-002-bailey-borwein-crandall-1997.md`)

---

## 0. TL;DR

The M1.2-frozen hybrid basis is

```
B_D(C)  =  {1, K_0, K_0², ..., K_0^D, log K_0}  ∪  {K_0·c : c ∈ C}
```

with operator-fixed `D ≤ 6` and a height bound `H` to be specified in M2.3, all at 500 dps.

**BBC 1997 §4 has already PSLQ-null-tested the pure-power sub-basis `{1, K_0, K_0², …, K_0^50}` at much stronger parameters (D = 50, H = 10^70, 7350 dps).** Therefore the pure-power portion of B_D(C) at D ≤ 6 / 500 dps is a **strict weakening** of BBC's published null.

Consequently, the M2.3 success predicate **must** target the rows where the present mission produces genuinely new information — namely:

1. The `log K_0` row in isolation against the pure-power coordinates (BBC tested log K_0 inside a 21-dim log-multiplicative basis, NOT against pure powers of K_0);
2. The `K_0·c` bilinear product rows for c ∈ C ⊆ {π, e, ln 2, ζ(2), ζ(3), G, γ, …} (BBC tested log-form Σ a_k log c_k, NOT bilinear K_0·c).

Failure to calibrate against BBC 1997 risks silently re-running a weaker version of BBC's Test 1 and producing a Zenodo deposit at M6 whose central null result is structurally dominated by published 1997 work. **This document is the calibration anchor that prevents that.**

---

## 1. The BBC 1997 §4 published nulls (recap)

| BBC Test | Basis | Dim | D | Height bound H | dps | Outcome |
|---|---|---|---|---|---|---|
| 1 (pure-power) | {1, K_0, K_0², …, K_0^50} | 51 | 50 | 10^70 | 7350 | NULL |
| 1 (pure-power, K_{-1}) | {1, K_{-1}, K_{-1}², …, K_{-1}^50} | 51 | 50 | 10^70 | 7350 | NULL |
| 2 (log-multiplicative) | {log K_0, log p_1..log p_15, log π, log e, log γ, log ζ(3), log log 2} | 21 | 1 (linear) | 10^20 | 7350 | NULL |
| 2 (log-multiplicative, K_{-1}) | same as above with K_{-1} | 21 | 1 (linear) | 10^20 | 7350 | NULL |

**BBC §4 close (explicit OPEN problem named):**
> "further work might be to rule out the possibility that log K_0, (log K_0)(log 2), or one of many other forms involving K_0 be an algebraic number of low degree."

This is BBC explicitly handing the present mission its publishable novelty axis.

---

## 2. Sub-family-by-sub-family comparison

### 2.1 Pure-power portion `{1, K_0, K_0², …, K_0^D}`

| Aspect | BBC 1997 Test 1 | This mission (B_D(C) restricted to pure powers) |
|---|---|---|
| Degree D | 50 | ≤ 6 |
| Height H | 10^70 | TBD in M2.3, almost-certainly ≤ 10^? smaller |
| dps | 7350 | 500 |
| Result | NULL (any relation requires |a_k| > 10^70) | Will trivially also be NULL (strict subset of BBC's basis at weaker parameters) |

**Status: BBC-grandfathered.** Reproducing this null is not a publishable novelty. The M3.1 harness should report it as **reproduction of BBC 1997 §4 Test 1 at lower precision**, NOT as a new finding. M2.3 success predicate **must not** make the pure-power null its principal result.

**M2.3 implementation rule (proposed):** the harness emits a "BBC-pure-power reproduction" diagnostic for the {1, K_0, …, K_0^D} sub-basis as a sanity check, but the M2.3 success-or-failure criterion is defined ONLY on the rows below.

### 2.2 `log K_0` row in isolation against pure-power K_0 coordinates

| Aspect | BBC 1997 Test 2 | This mission (log K_0 row of B_D(C)) |
|---|---|---|
| Basis form | {log K_0, log p_1, …, log p_15, log π, log e, log γ, log ζ(3), log log 2} | {1, K_0, K_0², …, K_0^D, log K_0} (NO additional log-coordinates) |
| log K_0 isolated? | NO (mixed with 20 other log-coordinates) | YES (pure-power K_0 coordinates + single log K_0) |
| Question being asked | "Is log K_0 in the Q-span of log of standard primes/constants?" | "Is log K_0 algebraic over Q[K_0]?" (or polynomial in K_0 with integer coefficients) |
| BBC outcome | NULL at H = 10^20 | This mission's M3.1 sweeps will determine. |

**Status: CLEAN NOVELTY.** BBC explicitly named `log K_0 algebraicity of low degree` as open. The present mission's basis isolates exactly that question.

### 2.3 `K_0·c` bilinear rows for c ∈ C

| Aspect | BBC 1997 Test 2 | This mission (K_0·c rows of B_D(C)) |
|---|---|---|
| Form | log-form: Σ a_k log c_k (additive in logs) | bilinear: K_0 · c (multiplicative in primal) |
| Distinction | Σ a_k log c_k = 0 ⇔ Π c_k^{a_k} = 1 (multiplicative integer relation among the constants themselves) | Linear relation Σ a_k (K_0 · c_k) = b, with c_k ∈ {π, e, ln 2, ζ(2), ζ(3), G, γ, …}; tests whether K_0 lies in the Q-span of {c_k^{-1}} mod a constant offset |
| BBC outcome | NULL at H = 10^20 on log-form | This mission's M3.1 sweeps will determine on bilinear form. |

**Status: CLEAN NOVELTY.** Bilinear K_0·c form is structurally distinct from BBC's log-form. No BBC null applies.

---

## 3. Proposed M2.3 success-predicate calibration (draft for operator review at M2.3)

> **M2.3 success predicate (calibrated):** The M3.1 PSLQ sweep is _informative_ — i.e., produces information not already published in BBC 1997 — if and only if it reports:
>
> **(A)** a verified integer relation OR a verified null at height-bound H_target ≥ 10^? on **at least one** of:
>   - the `log K_0` row in isolation against {1, K_0, K_0², …, K_0^D}, **OR**
>   - any of the `K_0·c` bilinear rows for c ∈ C
>
> with c chosen from a fixed named-constant basis to be specified in M2.3 §C-set definition.
>
> **(B)** The pure-power sub-basis {1, K_0, K_0², …, K_0^D ≤ 6} is reported as a **BBC-1997 reproduction diagnostic**, not as a primary result.
>
> **(C)** Height-bound H_target should be calibrated against the operator's precision budget (500 dps). A pure-precision argument gives an upper bound: for a basis of dimension k, PSLQ can certify nulls up to roughly H ≈ 10^(precision_digits / k). At 500 dps and basis dim ~10 (B_D(C) with D=6, |C|=3), this gives H ≈ 10^50. This is WEAKER than BBC's 10^70 on pure-power Test 1 (k=51) but stronger than BBC's 10^20 on log-multiplicative Test 2 (k=21). The fact that on the **novel rows** the present mission achieves stronger H than BBC's published nulls (because the basis dimension is smaller) is itself the warrant for a publishable result.

The above is a CLI **proposal**, not a binding M2.3 specification. M2.3 is the milestone where the operator fixes the formal predicate.

---

## 4. C-set definition (proposed for M2.3)

The set C of named constants appearing in the bilinear `K_0·c` rows is currently underspecified in `selected.md` §2 (which says "anticipated C-members"). M2.3 should fix C explicitly. CLI proposes:

```
C  =  {π, e, ln 2, γ, ζ(2), ζ(3), G}
```

- **|C| = 7** (within operator's machinery budget; PSLQ at 500 dps on basis dim ≤ 8 + |C| = 15 is well-tested in the signature paper at 300/600 dps)
- **All seven are computable to 500 dps via mpmath builtins** (verified at U-MISSION-H retroactive re-verification 2026-05-15, see capability/mpmath.available.md)
- **All seven are widely-attested in the BBC 1997 log-multiplicative Test 2 basis** (except G = Catalan's, which BBC did not include; adding G is a minor novelty extension)
- **All seven are arithmetic-nature-OPEN** (none has a known algebraic relation that would force a positive K_0·c result for trivial reasons)

Exclude (per `selected.md` §2.A scope-exclusion): the Khinchin-signature statistic S_n family. That family is ceded to lit-001.

Exclude (per `seeds/26_gauss_kuzmin_reframings_future_work.md` DO-NOT-REENTER): Gauss-Kuzmin-Wirsing operator-derived constants (Wirsing's constant 0.30366…, etc.).

---

## 5. Reference closure (citations downstream)

When the M3.1 harness emits its PSLQ sweep results, the M3.1 results MUST cite:

- `lit-002` (BBC 1997) as the pure-power-and-log-multiplicative grandfathering reference
- `lit-001` (signature paper) as the prior-art legitimacy anchor and methodology source for the precision cascade
- `lit-003` (OEIS A002210) as the K_0 numerical-value cross-check (mpmath at 500 dps vs OEIS 110-digit b-file)

These three citations are the **minimum citation set for a publishable M3.1 result.**

---

## 6. Outstanding question for operator (M2.3 phase, NOT M2.1)

- **Q-M2.3-A:** Does the operator accept the calibration in §3 above, or override with a different success-predicate definition?
- **Q-M2.3-B:** Does the operator accept C = {π, e, ln 2, γ, ζ(2), ζ(3), G} or specify a different C-set?
- **Q-M2.3-C:** Height-bound H_target for the novel rows — accept the heuristic 10^50 from §3, or specify a different value?

These are M2.3-phase questions, surfaced here at M2.1 so the operator has the BBC-grandfathering context when those decisions arrive.

---

## 6.5 M2.3 BINDING REQUIREMENT (operator directive, U-MISSION-J 2026-05-15 ~21:00 JST)

**Source:** U-MISSION-J operator reply at M2.1 closure, verbatim:

> "M2.3 binding requirement: the success predicate must explicitly exclude
> the pure-power family {1, K_0, K_0^2, ..., K_0^D} tested in BBC 1997 §4
> Test 1 (dim 51, D≤50, H≤10^70, 7350 dps). The hybrid basis B_D(C)
> remains; the pure-power-only subset is named as grandfathered prior art
> and the predicate scope is the complement."

This is a **binding** directive on the M2.3 success-predicate text — not a CLI proposal subject to operator review at M2.3, but a pre-bound requirement that the M2.3 predicate must satisfy. The §3 proposal above is consistent with this directive (and was its basis); this §6.5 makes the binding explicit so that M2.3-time drafting cannot accidentally weaken it.

**Concretely, the M2.3 predicate text must include language equivalent to (CLI proposal, subject to M2.3-phase polish):**

> "The success predicate is defined on the basis B_D(C) **minus the pure-power-only subset** {1, K_0, K_0², …, K_0^D : D ≤ 6}. The pure-power-only subset is named as **grandfathered prior art** of Bailey-Borwein-Crandall 1997 §4 Test 1 (basis dim 51, D ≤ 50, height ≤ 10^70, precision 7350 dps); reproducing a null on this subset at the operator's strictly-weaker parameters (D ≤ 6, 500 dps, height bound TBD) is treated as a **BBC-1997 reproduction diagnostic**, not as a primary result. The predicate's success-criterion is therefore evaluated on the complement: the `log K_0` row in isolation against the pure-power coordinates, and the `K_0·c` bilinear rows for c ∈ C, where C = {π, e, ln 2, γ, ζ(2), ζ(3), G} (or a sub-set thereof per M2.3 §C-set fix)."

This text is a CLI draft for the M2.3 predicate; M2.3-phase final wording is the operator's call, but the **structural content** above (basis-minus-pure-power-subset, BBC-grandfathering naming, complement-scope) is operator-bound.

---

## 6.6 Scope-positioning note for M6 (operator directive, U-MISSION-J 2026-05-15)

**Source:** U-MISSION-J operator reply, verbatim:

> "Scope-positioning note for M6: the manuscript now has a two-anchored legitimacy story. khinchin-signature-pslq (operator's own 2026-04-17 paper) handed off algebraic-combination relations; BBC 1997 §4 explicitly named log K_0, (log K_0)(log 2), and 'many other forms involving K_0' as open. The mission's refined sub-question sits inside both handoffs. This is the strongest possible scope-positioning for a partial-result paper and should be cited explicitly in M6 §Introduction."

**Captured here for M6 reference.** The two-anchored legitimacy story is:

| Anchor | Source | Quoted passage / scope handoff | M6 §Introduction role |
|---|---|---|---|
| **Anchor 1 — operator's own signature paper handoff** | `lit-001` papanokechi/khinchin-signature-pslq (2026-04-17 draft) | "Templates involving n^{1/2}, n^2, or **algebraic combinations of K_0 with other known constants** lie entirely outside the tested family and remain open." | Legitimacy for working on K_0 algebraic combinations — direct out-of-scope-but-open handoff from operator's own prior published work |
| **Anchor 2 — BBC 1997 §4 close handoff** | `lit-002` Bailey-Borwein-Crandall 1997 | "further work might be to rule out the possibility that **log K_0, (log K_0)(log 2), or one of many other forms involving K_0 be an algebraic number of low degree**." | Legitimacy for the specific log K_0 and bilinear K_0·c rows — direct out-of-scope-but-open handoff from foundational 1997 BBP-class work |

The bounded sub-question's basis B_D(C) sits **inside both handoffs**:
- The `log K_0` row of B_D(C) is named open by Anchor 2.
- The `K_0·c` bilinear rows for c ∈ C are subsumed under "one of many other forms involving K_0" (Anchor 2) and "algebraic combinations of K_0 with other known constants" (Anchor 1).

**M6 §Introduction obligation:** the M6 manuscript's introduction must cite both anchors explicitly (with the passages quoted as above) within the first three paragraphs, framing the present mission's bounded sub-question as the intersection of the two handoffs.

This is the operator's framing of the strongest possible scope-positioning for a partial-result paper, and it is now binding on M6 drafting.

---

## 7. AEAL discipline note

This document is **content**, not process-scaffolding (per the M1.1→M1.2 process-to-content binding rule):

- It cites a paper-read-verified primary source (lit-002).
- It produces a specific calibration claim (pure-power sub-basis is BBC-grandfathered at strictly stronger parameters).
- It proposes specific M2.3 success-predicate language (§3, §4) for operator review at M2.3.
- It enumerates specific cross-citation requirements (§5).

It does NOT meta-describe how the ledger is structured (that's `_schema.md`, a one-time scaffold per operator's allowance).

It does NOT propose changes to `selected.md` (which is frozen at gold/M1).
