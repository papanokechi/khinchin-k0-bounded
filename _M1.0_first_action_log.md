# Unsolved-Relay — M1.0 First-Action Audit Log

**Mission Brief reference:** operator-paste 2026-05-15 ~17:26 JST
**Framework:** AEAL v1 + SIARC relay
**Repository (intended):** `papanokechi/unsolved-relay` — NOT YET CREATED (rule-6 / Tier-3b operator-gated)
**Staging location:** `siarc\unsolved-relay-staging\` (this file is at staging root)
**Authoring session:** Copilot CLI `72100405-1480-4d08-9540-2727260983ba`

---

## §1 Confirmation of reading §0–§8 of the Mission Brief

I have read sections §0 through §8 of the Mission Brief in full and accept the binding constraints. The non-negotiable items I have internalized:

1. **§0.1 Seven-field JSON entries** for every claim in `claims/`. ID + statement + evidence_class + precision_or_dependencies + reproduce_command + independent_verifier_result + status. No exceptions.

2. **§0.2 Capability Gap protocol is supreme.** Writing `capability/<x>.gap.md` and stopping is a SUCCESSFUL outcome. Substituting a sweep for missing machinery is the named failure mode (Searcher's Fatigue). I will not do this. If precision, library, or theorem is unavailable, I document and HALT.

3. **§0.3 Negative results = first-class deliverables.** "This approach does not work at this precision via this method, here is the reproduce command" is a milestone, not a failure. No retry once the gap is documented.

4. **§0.4 Agent Health Score** per session on three axes: claim-to-sweep ratio ≥ 0.8, reproduce-command coverage ≥ 0.8, capability-gap honesty ≥ 0.8.

5. **§1.1 hard filter on M1.1 candidates.** Machinery must have been previously deployed by the operator. No new machinery in the survey set. (Applied — see §3 below.)

6. **§7 anti-thrashing rules**: one hypothesis mutation per milestone, no restart pre-Gold-Freeze without operator approval, mission TERMINATES at two consecutive capability gaps (negative result IS the deliverable).

7. **§8 prohibitions**: no "solved" / "breakthrough" / "novel" / "first" language. No LLM-generated proof steps un-checked by Lean 4 or CAS. No verification-harness skipping. No machinery-extrapolation. No advance past documented capability gap.

8. **§9 sequence**: deliver (a) confirmation, (b) validator, (c) survey set — then HALT. No M1.1 triage rows, no M1.2 scoring, no M1.3 Gold Freeze until operator approves the survey set.

I also note the tension between this mission's strict AEAL discipline and the just-prior SIARC rejection-events absorption cycle (where Item 28 JNT referee comment surfaced the `external_validation_deficit` signal). The new mission's design — bounded sub-questions, citation-graph-independent track, AEAL-anchored verification harness — is well-suited to address the dimensions surfaced by that signal. This is a design feature; the mission is sound in light of the recent corroboration.

---

## §2 Deliverable (b): triage_validator.py + triage_schema.json

Staged at:
- `siarc\unsolved-relay-staging\targets\triage_schema.json` — JSON Schema Draft 2020-12 for one triage row
- `siarc\unsolved-relay-staging\targets\triage_validator.py` — pure-Python validator with two modes (jsonschema if available; pure-stdlib fallback) + `--self-test` + `--strict` flag

**Self-test verification (executed 2026-05-15 ~17:32 JST):** PASS on 5 fixture cases (good row, missing-field, machinery-unavailable warning, machinery-unavailable strict-mode error, hours-out-of-range).

**Reproduce command:**
```powershell
cd siarc\unsolved-relay-staging\targets
python triage_validator.py --self-test
# expected: "self-test: PASS"
```

**Schema coverage:** all 9 fields per Mission Brief §M1.1 JSON example. Plus enum constraints on `category` (8 values), `publishability_of_negative_result` (3 values), `AEAL_compliance_risk` (3 values). Plus URI format check on `known_partial_results_url`. Plus hard cap of 840 CPU-hours on `estimated_compute_to_partial_result_hours` (= 5 weeks × 168 h/week, per the §M1.2 falsifiability-window constraint).

**AEAL extras** (warning in default mode, error in `--strict`):
- `machinery_available_locally=false` flagged: row MUST NOT advance to M1.2.
- `AEAL_compliance_risk=high` flagged: re-justify before advancing.

**Independent verifier:** the self-test serves this role for the validator itself; for actual triage rows, the validator IS the independent verifier (per §0 evidence_class=`literature`-or-`symbolic`).

---

## §3 Deliverable (c): targets/survey_set.md

Staged at `siarc\unsolved-relay-staging\targets\survey_set.md`. **30 candidate problems**, organized in 6 categories of 5 each:
- A. Number theory — Diophantine equations
- B. Number theory — multiplicative / primes
- C. Continued fractions & Diophantine approximation (NON-PCF)
- D. Transcendence / irrationality (PSLQ-driven)
- E. Partition theory & combinatorics
- F. Dynamics & ergodic theory (NON-Painlevé-III-overlapping)

**Exceeds §M1.1 minimum of 25 by 5; spans 5+ subject categories.**

**Hard filter applied:** §M1.1 sentence 3 ("machinery you have not previously deployed successfully"). Operator's verified base = {PSLQ, mpmath, Lean 4 + Mathlib4, PCF spectral fingerprint framework, Painlevé / Stokes tooling}. Excluded: alg-geom-over-schemes, modular-forms-beyond-q-expand, hyperbolic-geom, low-dim-topology, fluid-mechanics, PCP, Riemannian-flow, formal-logic, rep-theory. Three entries (#7, #22, #25) included with explicit flag that the FULL proof would invoke missing machinery — they survive only as bounded-VERIFICATION targets and will be marked `machinery_available_locally=false` at M1.1 (or the bounded scope will be rescoped to only the verification-not-proof slice).

**Independence from active R1 / Painlevé III queue:** survey deliberately under-weights direct-PCF and direct-Painlevé-III problems. Entries #26 (Gauss–Kuzmin error terms) and #23 (plane-partition asymptotic constants) flagged for operator review as having mild active-work overlap.

**No scoring in this file** (per §9): no tractability rank, no machinery-fit score, no publishability score, no AEAL-risk score. Scoring happens at M1.1 triage-row construction AFTER operator approves the survey set.

---

## §4 HALT condition (per Mission Brief §9)

This session has now produced (a)/(b)/(c) of the §9 first action. Per §9 final sentence:

> *"Do not begin M1.2 until the operator approves the survey set."*

I interpret this conservatively as: **do not begin M1.1 triage-row construction either, until the survey set is approved.** Reasoning: M1.1 produces the triage rows that feed M1.2 selection; building 30 schema-valid rows before approval would waste effort if operator amends the survey set, and could pre-commit the framing.

**Operator actions pending:**
1. **Approve / amend / reject** `targets/survey_set.md` as the M1.1 input.
2. **Approve / decline** creation of `papanokechi/unsolved-relay` repo via `gh repo create` (and clone of structure from `papanokechi/congruent-relay`). This is rule-6 / Tier-3b operator-gated. CLI will not auto-create.
3. **Confirm / amend** the hard-filter machinery base; in particular call out anything not in the operator's verified machinery set that operator considers usable for this mission (e.g., is `arb` / `flint` accessible? is `pari/gp` available? is LLL via `fpylll` or `sage` accessible?).

**Until operator returns with approval(s):** no further M1 work. No GitHub interactions. No commits. No mutation of `survey_set.md`. The CLI will resume M1.1 triage-row construction immediately upon operator greenlight.

---

## §5 Three-axis Agent Health Score (M1.0 partial — only sections that apply at this stage)

This score will be carried forward through M2–M6 per Brief §0.4.

| Axis | M1.0 partial | Notes |
|---|---|---|
| claim-to-sweep ratio | **N/A at M1.0** | No claims minted yet; the survey set is a candidate list, not a claim. First claim will land in M1.1 (one per triage row asserting "machinery_required is the minimal set that suffices for the bounded sub-question"). |
| reproduce-command coverage | **1/1 = 1.0** | The single computable deliverable in M1.0 is the validator self-test; reproduce command IS supplied. |
| capability-gap honesty | **3 explicit flags** | Entries #7, #22, #25 flagged as machinery-gap; entries #10, #26 flagged as borderline; entry #18 flagged for literature-recheck. No problems were quietly included with hidden machinery gaps. |

**Composite at M1.0:** acceptable; full composite will be computed at the M6 handoff per Brief §6.3.

---

## §6 Cross-references to SIARC governance

- **Rule 6 (Tier 3b operator-gated authority):** preserved. No portal interactions, no `gh repo create`, no `git commit` to any tracked SIARC branch.
- **Standing Final Step B1-B5:** NOT fired. Operator-gated.
- **Premise 3 hygiene-rule (NEW today):** every portal-submit must trigger immediate log append. For the unsolved-relay mission, the analogue is: every `gh` operation that creates an external resource (repo, deposit, issue) must trigger an immediate audit-log entry in `mutation_log/` per Brief §7.
- **AEAL v1:** this mission's per-claim seven-field JSON is the unsolved-relay-specific instantiation of the SIARC `claims.jsonl` AEAL discipline.

---

**End of M1.0 first-action audit log.**
*Awaiting operator §9 approval before M1.1 triage execution.*
